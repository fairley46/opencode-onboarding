{
  "name": "opencode-onboarding",
  "version": "0.2.0",
  "description": "OpenCode onboarding workspace — open in OpenCode to begin guided AI onboarding.",
  "private": true,
  "type": "commonjs",
  "scripts": {
    "setup": "node setup.js",
    "test": "node --test"
  },
  "engines": {
    "node": ">=20"
  }
}
